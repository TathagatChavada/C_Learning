#include <stdio.h>

int main()
{
    // int start, end, i, sum;

    // sum = 0;

    // printf("Enter the starting value: ");
    // scanf("%d", &start);

    // printf("Enter the ending value: ");
    // scanf("%d", &end);

    // for (i = start; i<=end; i++)
    // {
    //     sum += i;
    // }

    // printf("The sum of numbers between the given range is: %d", sum);

    int a = 10;

    printf("%d", a);
}